import { Calendar, Users, Code2, Trophy, Presentation, PartyPopper, type LucideIcon } from 'lucide-react';

export const iconMap: Record<string, LucideIcon> = {
  Calendar,
  Users,
  Code2,
  Trophy,
  Presentation,
  PartyPopper,
};